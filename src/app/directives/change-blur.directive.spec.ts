import { ChangeBlurDirective } from './change-blur.directive';

describe('ChangeBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBlurDirective();
    expect(directive).toBeTruthy();
  });
});
